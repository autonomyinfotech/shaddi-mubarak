<section style="background-image: url(/assets/pass.jpg)">
<div class="container">
  <div class="row">
    <div class="col-md-8"></div>
    <div class="col-md-4">
      <div class="panel" style="display:initial;">
        <div class="panel-body">
            <h2 style="color:#fff;"><%= t('forgot_password')%></h2>
        </div>
        <div class="panel-footer">
          <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

          <%= devise_error_messages! %>

            <div class="form-group">
            <%= f.email_field :email, autofocus: true, type: "text", class: "form-control", required: true, placeholder: "#{t('email')}" %>
            </div>

    	      <div class="form-group">
    	      <%= f.submit "#{t('reset_password')}", class: "btn btn-primary btn-lg" %>
            </div>    
  	    	  <%= render "devise/shared/links" %>
  	      <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
</section>